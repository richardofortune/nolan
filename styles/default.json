{
  "$comment": "PRESENTATION. Nothing here says what happens — only how it looks and how fast. Edit this file and every screenplay you own re-renders with the new look. This is the document you iterate on over time.",

  "pace": 1,

  "timing": {
    "$comment": "Durations are DERIVED, not hardcoded, so the same screenplay works with typed captions or spoken voiceover. readingSpeed is ms per character; a future 'voice' mode replaces this with real audio length and every beat re-times itself.",
    "mode": "caption",
    "readingSpeed": 22,
    "minCaption": 900,
    "leadIn": 600,
    "beatOut": 800,
    "typeDelay": 24,
    "glideDefault": 650,
    "settle": 250
  },

  "caption": {
    "$comment": "Flat scalar fields — one axis at a time, no CSS shorthand to re-parse (shape borrowed from Cap). `preset` picks a whole look; fields here override it. `variants` are partial, semantic looks a `say` beat opts into with `as`. The screenplay names the intent (`as:\"warn\"`); this file owns the colours.",
    "preset": "docs",

    "position": "bottom-center",
    "height": 54,
    "padding": "12px 22px",

    "font": "-apple-system,BlinkMacSystemFont,sans-serif",
    "fontWeight": 500,
    "size": 18,
    "lineHeight": 1.35,
    "uppercase": false,
    "italic": false,

    "color": "#F2F3F5",
    "backgroundColor": "#16181D",
    "backgroundOpacity": 100,
    "outline": false,
    "outlineColor": "#000000",
    "fadeDuration": 0.2,

    "caret": "▍",
    "showChip": true,

    "activeWordHighlight": false,
    "highlightColor": "#FFD400",
    "highlightStyle": "color",
    "wordTransitionDuration": 0.25,

    "variants": {
      "warn":  { "backgroundColor": "#3A2A0B", "color": "#FFC53D" },
      "aside": { "color": "#9AA0AA", "showChip": false, "italic": true },
      "code":  { "font": "ui-monospace,Menlo,Consolas,monospace", "size": 16 }
    }
  },

  "presenter": {
    "$comment": "The 'person in the corner', like a screen-share webcam bubble. Off by default. When on, it shows the current actor — their `cam` photo if the cast entry has one, otherwise a coloured avatar of their initial. Burned in like the cursor, so it works in burn and post modes.",
    "show": false,
    "corner": "bottom-right",
    "size": 140,
    "shape": "circle",
    "margin": 22,
    "ring": true,
    "ringColor": "#ffffff",
    "ringWidth": 3,
    "shadow": true,
    "label": true
  },

  "cursor": {
    "show": true,
    "size": 22,
    "fill": "#16181D",
    "stroke": "#ffffff",
    "click": {
      "$comment": "The click highlight — a filled tap-flash plus expanding ring(s), so a press is legible even to a non-technical viewer. dot=filled flash, dotTo=its final size, rings=1 or 2 (sonar). See docs/style-events.md.",
      "show": true,
      "colour": "#FFC53D",
      "from": 14,
      "to": 62,
      "ms": 520,
      "width": 3,
      "dot": true,
      "dotTo": 34,
      "rings": 2
    }
  },
  "steps": {
    "$comment": "Progress rail for how-tos. Off unless show:true AND the screenplay supplies a top-level steps[] array; advance with `step` beats. See docs/style-events.md.",
    "show": false,
    "corner": "top",
    "margin": 16,
    "bg": "rgba(15,23,42,.82)",
    "ink": "#E5E7EB",
    "active": "#FFC53D",
    "done": "#22C55E"
  },

  "transition": {
    "curtain": "#0f1115",
    "fadeMs": 320,
    "titleFont": "600 22px -apple-system,BlinkMacSystemFont,sans-serif",
    "titleInk": "#e6e8ec"
  },

  "encode": {
    "$comment": "One recording pass, many outputs. Add entries to render more.",
    "viewport": { "width": 1000, "height": 700 },
    "subtitles": ["srt", "vtt"],
    "outputs": [
      {
        "name": "gif",
        "ext": "gif",
        "args": "-vf \"fps=12,scale=900:-1:flags=lanczos,split[a][b];[a]palettegen=max_colors=128[p];[b][p]paletteuse=dither=bayer:bayer_scale=4\" -loop 0"
      },
      {
        "name": "mp4",
        "ext": "mp4",
        "args": "-vf \"scale=1000:-2\" -c:v libx264 -pix_fmt yuv420p -crf 23 -movflags +faststart"
      }
    ]
  }
}
